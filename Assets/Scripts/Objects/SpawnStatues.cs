using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnStatues : MonoBehaviour
{
    /*
    [SerializeField]
    private GameObject statuePrefab;
    [SerializeField]
    private int numStatues;

    void Start()
    {
        
    }

    void Update()
    {
        int i = numStatues;

        Vector3 randomPosition = new Vector3(new random.Range(0, 10), 0, new random.Range(0, 10));
        
        while (i>0){
            GameObject.Instantiate(statuePrefab, randomPosition, Quaternion.identity);
            i--;
        }
    }
    */
}
